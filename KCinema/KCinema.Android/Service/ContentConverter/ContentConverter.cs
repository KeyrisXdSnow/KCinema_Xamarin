using System.Collections.Generic;
using Android.Media;
using Android.Util;
using Android.Widget;
using KCinema.model;
//using Xamarin.Protobuf.Lite;

namespace KCinema.Android.Service.ContentConverter
{
    public class ContentConverter
    {
        
        // public Dictionary<string, Any?> ToMap(Content content)
        // {
        //     var result = new Dictionary<string, Any?> {["name"] = (Any) content.Name, ["contentType"] = (Any) content.Type.ToString()};
        //     
        //     if (content.Genres != null) result["genres"] = (Any) content.Genres;
        //     if (content.Rating != 0) result["rating"] =  (Any) content.Rating;
        //     if (content.Country != null) result["country"] = (Any) content.Country;
        //     if (content.WatchTime != 0) result["watchTime"] = (Any) content.WatchTime;
        //     if (content.ReleaseDate != null) result["releaseDate"] = (Any) content.ReleaseDate;
        //     if (content.PosterUrl != null) result["posterUrl"] = (Any) content.PosterUrl;
        //     if (content.TrailerUrl != null) result["trailerUrl"] = (Any) content.TrailerUrl;
        //     if (content.Description != null) result["description"] = (Any) content.Description;
        //     if (content.Gallery != null) result["gallery"] = (Any) content.Gallery.ToArray();
        //
        //     return result;
        //
        // }
    }
}